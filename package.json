{
  "name": "resume-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:thoughts": "node scripts/build-thoughts.mjs",
    "build:sitemap": "node scripts/build-sitemap.mjs",
    "build:css": "tailwindcss -i ./src/input.css -o ./public/style.css",
    "build": "npm run build:thoughts && npm run build:sitemap && npm run build:css",
    "watch": "tailwindcss -i ./src/input.css -o ./public/style.css --watch",
    "deploy": "npm run build && wrangler pages deploy ./public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "wrangler": "^4.59.1"
  }
}
